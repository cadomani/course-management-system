{
  "name": "cms",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "concurrently": "^6.2.1"
  },
  "scripts": {
    "start": "concurrently 'npm start --prefix ./backend' 'npm start --prefix ./frontend'",
    "backend-build": "cd backend && npm run build",
    "frontent-build": "cd frontend && npm install && npm run build",
    "build": "npm install && npm run frontend-build && npm run backend-build"  
  },
  "workspaces": [
    "./backend/*",
    "./frontend/*"
  ],
  "engines": {
    "node": "16.10.x",
    "npm": "7.24.x"
  }
}
