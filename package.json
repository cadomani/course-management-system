{
  "name": "cms",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "concurrently": "^6.2.1"
  },
  "scripts": {
    "start": "concurrently 'PORT=5070 npm start --prefix ./backend' 'PORT=5080 npm start --prefix ./frontend'",
    "build": "NODE_VERBOSE=true && concurrently 'cd frontend && npm run self-install' 'cd backend && npm run self-install'"
  },
  "engines": {
    "node": "16.10.x",
    "npm": "7.24.x"
  }
}
