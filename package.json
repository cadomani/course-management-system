{
  "name": "cms",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "npm start --prefix ./backend",
    "runbackend": "npm build --prefix ./backend",
    "runfrontent": "npm start --prefix ./frontend",
    "build": "npm run runbackend"  
  },
  "workspaces": [
    "./backend/*"
  ],
  "engines": {
    "node": "16.10.x",
    "npm": "7.24.x"
  }
}
