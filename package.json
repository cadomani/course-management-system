{
  "name": "cms",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "runbackend": "npm start --prefix ./backend",
    "runfrontent": "npm start --prefix ./frontend",
    "build": "npm install && npm runbackend"  
  }
}
