const colors = require('tailwindcss/colors')

module.exports = {
  mode: 'jit',
  theme: {
    extend: {
      colors: {
        sky: colors.yellow,
        cyan: colors.cyan,
      },
    },
  },
  variants: {},
  plugins: [],
}
